<%- include("./partials/navigation", {title : "ENROLLED STUDENTS"}) %>
	<h1> <%= subject[0].Subject.subject_name %> </h1>
	<h4> <a href ="/subjects"> return to subject list</a>	</h4>	
	<table border="1" width="70%">
		<thead>
			<tr>
				<th> No </th>
				<th> name </th>
				<th> score </th>
				<th> Nilai </th>
			</tr>
		</thead>
		<tbody>
			<% subject.forEach((student,index) => {%>	
			<tr>
				<td> <%= index+1 %> </td>
				<td> <%= student.Student.getFullName() %></td>
				<% if (student.score == null) {%>
				<td><a href="/subjects/<%= student.id %>/givescore"> Give Score </a></td>
				<%}else{%>
				<td> <%= student.score %> </td>
				<%}%>
				<td> <%= student.scoreAlphabet %> </td>
			<%})%>
			</tr>	
		</tbody>	
	</table>	
</body>
</html>