<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="../../assets/css/bulma.min.css" type="text/css">
  <link rel="stylesheet" href="../../assets/css/mystyle.css" type="text/css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" type="text/css">
  <title>SchoolApp-Enrolled-Students</title>
</head>
<body>

  <div class="tabs is-toggle is-fullwidth">
    <ul>
      <li><a href="/subjects"><span class="icon">
        <i class="fa fa-user"></i>
      </span>
      <span>BACK SUBJECT</span>
    </a></li>
    </ul>
  </div>

  <div class="container_enrolled">

    <div style="a:2px; text-align:left; margin-bottom:25px;">
      <% for(let i=0; i<StudentSubjectWithStudent.length; i++) {%>
      <% enroll = ''
      enrolll = ''
      if(StudentSubjectWithStudent[i].SubjectId == data_Subjects.id){
        enrolll = 'Enrolled' + ' ' + data_Subjects.subject_name + ' ' + ' Subject Data'
      }else{
        enroll = "Belum ada siswa terdaftar, masukkan siswa ke subject. Jika sudah silahkan kembali ke halaman ini."
      }%>
      <%}%>
        <p class="title is-4" style="text-align:center; margin-bottom:13px;"><span><%=enrolll%></span> <span style="color:#ff3860;"><%=enroll%></span> </p>
    </div>

    <table class="table is-striped is-fullwidth">
        <tr>
          <th><abbr title="Position">NO</abbr></th>
          <th>STUDENT NAME</th>
          <th style="width:120px; text-align:center;">SCORE</th>
          <th style="width:99px; text-align:center;">DESC</th>
        </tr>

        <% for(let i=0; i<StudentSubjectWithStudent.length; i++) {%>
          <%if(StudentSubjectWithStudent[i].SubjectId == data_Subjects.id){%>
          <tr>
            <td>
              <%=[i+1]%>
            </td>

            <td>
              <%if(StudentSubjectWithStudent[i].SubjectId == data_Subjects.id){%>
                <%=`${StudentSubjectWithStudent[i].Student.first_name} ${StudentSubjectWithStudent[i].Student.last_name}`%>
              <%}%>
            </td>
            <td>
              <a href="/subjects/<%=StudentSubjectWithStudent[i].id%>/givescore" class="button is-small"style="width:120px; text-align:center;">Give Score</a>
            </td>
            <td>
              #
            </td>
          </tr>
          <%}%>
        <%}%>

      </table>
  </div>
</body>
</html>
