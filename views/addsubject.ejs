<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>HALAMAN STUDENT</title>

    <style type = "text/css">

      .tabel {
        background-color: brown;
        font-family: Calibri;
        width : 100%;
      }

      .warna {
        color : white;
      }

      .rapi {
        color: white;
        text-align:  center;
        font-family: cursive;
      }

      fieldset {
        width : 30%;
      }

      a {
        color: white;
        font-family : Arial;
        text-decoration: none;
      }

      a:hover {
        color : grey;
      }

    </style>

  </head>
  <body style="background-color : aquamarine">
    <fieldset>
      <legend><b> ADD SUBJECT </b></legend>
      <table border = "1" class = "warna tabel rapi">
          <tr>
            <td><b>First Name</b></td>
            <td><%= rowStudents.first_name %></td>
          </tr>

          <tr>
          <td><b>Last Name</b></td>
          <td><%= rowStudents.last_name %></td>
          </tr>

          <tr>
            <td><b>E-mail</b></td>
            <td><%= rowStudents.email %></td>
          </tr>

      </table>

      <form action = "/students/<%= rowStudents.id %>/addsubject" method ="post">
      <table border = "0">
        <tr>
          <td></td>
            <td>
              <select name = "SubjectId">
                  <option>------SELECT-----</option>
                  <% for(let i = 0 ; i < rowSubjects.length ; i++) { %>
                    <option value = "<%= rowSubjects.id %>"><%= rowSubjects[i].subject_name %></option>
                  <% } %>
              </select>
            </td>

            <td>
              <input type="submit" value="ADD SUBJECT">
            </td>
        </tr>
      </table>
    </form>
  </fieldset>
  </body>
</html>
